// ---------------------------------------------------------------------------
// src/client/index.js
// Client module entry — exports the client routes.
// ---------------------------------------------------------------------------
const clientRoutes = require('./routes/client.routes');

module.exports = {
  clientRoutes
};
