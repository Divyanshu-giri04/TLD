// ---------------------------------------------------------------------------
// src/shared/index.js
// Shared module entry — exports the shared/public routes.
// ---------------------------------------------------------------------------
const sharedRoutes = require('./routes/shared.routes');

module.exports = {
  sharedRoutes
};
